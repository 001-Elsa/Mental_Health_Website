[pytest]
markers =
    e2e: browser tests that require running API and frontend servers
addopts = -m "not e2e"
filterwarnings =
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning:jose.jwt
